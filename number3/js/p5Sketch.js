function setup() {
    createCanvas(600, 400);
               background(0);
}

function draw() {

    // direct manupulation

    ellipse(mouseX, mouseY, 60)
        rect(mouseX, mouseY, 60)
    

    // // constraint
    //     let x = constrain(mouseX, 100, 500);

    //     ellipse(x, 200, 50, 50)

}