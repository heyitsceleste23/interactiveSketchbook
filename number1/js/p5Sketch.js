function setup() {
    createCanvas(600, 400);
       background(100);
}

function draw() {

    // direct manupulation

    ellipse(mouseX, mouseY, 60)

    // constraint
        // let x = constrain(mouseX, 100, 500);

        // rect(x, 200, 50, 50)

}